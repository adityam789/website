export const dmpConfig = {
  /**
   * 'scroll' - Standard long page scrolling.
   * 'locked' - Sections are hidden until the "Proceed" button is clicked.
   */
  navigationMode: 'locked' as 'scroll' | 'locked'
}
